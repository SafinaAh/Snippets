class Solution {
 public int solution(int N) {
  int sqrt = ((Double) Math.sqrt(N)).intValue();
  int count = 0;
  while (begin > 0) {
   if (N % begin == 0) {
    if (N / begin == begin) {

     count++;
    } else
     count = count + 2;
   }
   begin--;
  }
  return count;

 }
}
